syntax = "proto3";

package skill;
option go_package="./skill";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Skill {
  rpc Ping(Request) returns(Response);
}
